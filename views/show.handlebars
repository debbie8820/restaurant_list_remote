<div class="container">
  <div class="d-flex flex-column align-items-center">
    <h1 class="py-4 restaurant-show-title">{{specificRestaurant.name}}</h1>
    <div class="d-flex justify-content-center">
      <a class="show editButton" href="/restaurants/{{specificRestaurant._id}}/edit">修改資訊</a>
      <a class="show deleteButton" href="/restaurants/{{specificRestaurant._id}}/delete">刪除餐廳</a>
    </div>
    <div class="col-12 col-md-10 mx-auto">
      <p class="mb-1">
        <span class="text-secondary">
          <i class="fas fa-star pr-1"></i>
          評價：
        </span>
        {{specificRestaurant.rating}}顆星
      </p>

      <p class="mb-1">
        <span class="text-secondary">
          <i class="fas fa-utensils pr-2"></i>
          類別：
        </span>
        {{specificRestaurant.category}}
      </p>

      <p class="mb-1">
        <span class="text-secondary">
          <i class="fas fa-map-marker-alt pr-2"></i>
          地址：
        </span>
        {{specificRestaurant.location}}
        <a href={{specificRestaurant.google_map}} target="_blank">
          <i class="fas fa-location-arrow pr-2 fa-xs"></i>
        </a>
      </p>

      <p class="mb-1">
        <span class="text-secondary">
          <i class="fas fa-mobile-alt pr-2"></i>
          電話：
        </span>
        {{specificRestaurant.phone}}
      </p>

      <p class="mb-5">
        {{specificRestaurant.description}}
      </p>
      <img class="rounded mx-auto d-block mb-4 w-100" src="{{specificRestaurant.image}}"
        alt="{{specificRestaurant.name}}" style="max-width: 600px;">
    </div>
  </div>
</div>